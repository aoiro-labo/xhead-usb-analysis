// Reconstructed from XHEAD-STUDIO decompiled client (ilspycmd). Schema only, no vendor implementation code.
syntax = "proto3";

// ms_capture.proto — frame/packet capture objects (video/audio grabbers,
// desktop/window capture, core-audio capture, V4L2, FFmpeg-based capture;
// see msCaptureType in ms_base.proto for the full backend enumeration).

import "ms_base.proto";
import "ms_property.proto";

message msCapture {
  uint32 HandleID = 1;
  uint32 ObjectID = 2;
  msObjectType ObjectType = 3;
  string Name = 4;
  string Path = 5;
  msCaptureType CaptureType = 6;
  msStatus Status = 7;
  msContent Content = 8;
  repeated msProperty Properties = 9;
}
